export enum ENodeEnv {
    DEV = "development",
    PROD = "production",
    TEST = "test"
}

export enum EModelNames {
    USER = 'User',
}

export enum EUserRole {
    USER = 'user',
    ADMIN = 'admin'
}

export enum EGender {
  MALE = 'male',
  FEMALE = 'female'
}